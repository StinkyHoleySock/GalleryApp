<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/album_item"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/iv_last_image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@drawable/ic_launcher_background"
        android:scaleType="centerCrop"
        android:contentDescription="@string/preview_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_album_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/example_album_name"
        android:layout_marginStart="8dp"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="@id/iv_last_image"
        app:layout_constraintStart_toEndOf="@id/iv_last_image"/>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/tv_number_photos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/example_number_photos"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:textSize="16sp"
        app:layout_constraintStart_toEndOf="@+id/iv_last_image"
        app:layout_constraintTop_toBottomOf="@+id/tv_album_name" />

</androidx.constraintlayout.widget.ConstraintLayout>